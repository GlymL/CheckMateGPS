package com.example.demo;

/**
 * Clase auxiliar que simula un proceso o tarea secundaria en el sistema.
 */
public class Dot {

    /**
     * Constructor de la clase Dot que ejecuta una impresión en consola al ser instanciado.
     * Al ser un constructor sin parámetros, no recibe datos externos.
     * * @param void Este constructor no recibe parámetros (se anota para cumplir estándar de documentación estricto si tu linter lo exige, de lo contrario se puede omitir).
     * @return No devuelve ningún valor, ya que es un constructor de instancia.
     */
    public Dot() {
        System.out.println("La la, lala, lalalalalalla~");
    }
}